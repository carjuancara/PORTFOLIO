{
    "version": 2,
    "buildCommand": "pip install -r requirements.txt",
    "startCommand": "gunicorn portfolio.wsgi --bind 0.0.0.0:8000",
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/"
        }
    ]
}