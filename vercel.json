{
    "version": 2,
    "buildCommand": "uv venv .venv && source .venv/bin/activate && uv pip install . && python manage.py runserver",
    "routes": [
        {
            "src": "/(.*)",
            "dest": "server.py"
        }
    ]
}